# AI 커리어 코파일럿 — 프로젝트 컨텍스트

> 이 문서는 AI 코딩 에이전트(Claude Code / Codex)를 위한 프로젝트 설명서다.
> 작업 전 이 문서를 먼저 읽고, 여기 정의된 스택·범위·원칙을 벗어나지 말 것.

## 1. 제품 한 줄 정의
IT 취업 준비생이 관심 채용 공고를 붙여넣으면 → 공고가 요구하는 **기술 + 인재상**을 분해하고 → 사용자의 보유 역량과 갭을 분석해 → 마감일까지 남은 기간 안에 무엇을 먼저 채워야 할지(**필요 자격증·시험 일정 포함**) 타임라인으로 안내하는 AI 커리어 코파일럿.

## 2. 무엇이 다른가 (핵심 포지션)
- 자소서 첨삭이 아니다. 공고-역량 "매칭 점수"도 아니다.
- 기존 서비스(사람인·원티드)가 안 하는 **"매칭 이후의 준비 구간"**을 챙긴다.
- **점수가 아니라 액션.** 갭을 기술 단위로 분해해 마감일 기준 "다음 할 일"로 변환한다.
- **플랫폼 독립**: 어느 채용 사이트 공고든 사용자가 붙여넣으면 동작한다.
- **크롤링하지 않는다.** 사용자가 공고 텍스트를 직접 붙여넣는 방식. 약관·차단·유지보수 문제를 원천 회피.

## 3. 분석하는 3개 축
1. **하드 스킬(기술)**: 공고의 자격요건/우대사항에서 기술 추출 → 보유 역량과 갭 분석. (예: Python, Docker, AWS)
2. **인재상(소프트 요건)**: 공고의 인재상/우대 태도에서 키워드 추출. (예: 주도성, 협업, 학습 의지)
   - 인재상은 "충족/부족"으로 단정하지 않는다. **"이 회사가 강조하는 가치 → 네 경험 중 무엇으로 어필할지"** 방향 안내로 쓴다.
   - 기술 갭은 채우는 데 시간이 걸리지만, 인재상은 **이미 가진 경험의 표현 방식** 문제라 즉시 준비 가능하다. 두 축이 상호 보완된다.
3. **자격증**: 공고가 요구/우대하는 자격증을 식별 → 시험 일정·난이도·준비기간과 연결해 마감일 대비 현실성 판단.
   - 예: "정보처리기사 우대 → 다음 시험 O월, 준비 3개월 필요, 공고 마감이 그 전이라 이번엔 불가. 다음 목표로."

## 4. 기술 스택 (고정)
- 프론트엔드: **React** (Vite 기반, JavaScript)
- 백엔드: **FastAPI** (Python 3.11)
- DB: **PostgreSQL + pgvector** (임베딩 유사도 매칭 — 갭 분석의 핵심)
- ORM/마이그레이션: SQLAlchemy + Alembic
- LLM: OpenAI API (기술·인재상 추출, 갭→액션 변환)
- 로컬 개발: Docker Compose (DB 컨테이너)
- OS: Windows 개발 환경

> 위 스택을 임의로 교체하지 말 것. 새 의존성 추가 시 이유를 설명할 것.
> **CORS 주의**: React 개발 서버(5173)와 FastAPI(8000)가 다른 포트이므로, FastAPI에 CORS 미들웨어 설정이 반드시 필요하다.

## 5. 프로젝트 구조
```
project/
├── CLAUDE.md                 # 이 문서
├── README.md
├── .gitignore
├── .env.example              # 키 이름만 (실제 값 금지)
├── docker-compose.yml        # PostgreSQL + pgvector 컨테이너
│
├── backend/                  # FastAPI
│   ├── venv/                 # 가상환경 (커밋 금지)
│   ├── requirements.txt
│   ├── alembic/              # DB 마이그레이션
│   └── app/
│       ├── main.py           # FastAPI 진입점, CORS 설정
│       ├── config.py         # 환경변수 로드
│       ├── database.py       # DB 연결
│       ├── models/           # SQLAlchemy 모델
│       ├── schemas/          # Pydantic 스키마
│       ├── api/              # 라우터 (엔드포인트)
│       └── services/         # 비즈니스 로직
│           ├── extractor.py  # 공고 → 기술·인재상·자격증 추출 (LLM)
│           ├── matcher.py    # 임베딩 매칭, 갭 산출
│           └── planner.py    # 갭 → 타임라인 액션 변환
│
├── frontend/                 # React (Vite)
│   ├── node_modules/         # 커밋 금지
│   ├── package.json
│   ├── index.html
│   └── src/
│       ├── main.jsx          # 진입점
│       ├── App.jsx
│       ├── components/       # 재사용 UI 컴포넌트
│       ├── pages/            # 화면 단위
│       ├── api/              # 백엔드 호출 함수
│       └── styles/
│
└── data/                     # 정적 데이터 (코드 아님)
    ├── tech_synonyms.json    # IT 기술 동의어 사전
    ├── certifications.json   # IT 자격증 정보 (일정·난이도·준비기간)
    └── job_samples/          # 테스트용 공고 샘플 20~30개
```

## 6. 핵심 기능 (MVP)
1. **공고 → 기술 + 인재상 + 자격증 추출**: 공고 텍스트 입력 → LLM으로 JSON 구조화 → 동의어 사전으로 표기 정규화.
2. **역량 갭 분석**: 보유 역량 vs 요구 기술을 임베딩 유사도(pgvector)로 매칭 → 충족/부족/부분충족 분류 → 시각화. 인재상은 별도로 "어필 포인트"로 제시.
3. **타임라인 액션 배치**: 갭 + 자격증 시험 일정을 마감일 기준 우선순위로 정렬 → 실행 과제로 변환 → 진척 추적.

## 7. 데이터 범위 (좁히는 것이 정확도를 지킨다)
- **IT 직군만.** 다른 직군으로 확장하지 말 것.
- **텍스트 공고만.** 이미지 공고 파싱 시도 금지.
- **자격증은 IT 핵심 몇 개만 관리**: 정보처리기사, SQLD, ADsP, 빅데이터분석기사, 정보보안기사, 리눅스마스터 등. 전체 자격증 아카이브를 만들려 하지 말 것 — 데이터 유지 부담으로 프로젝트가 무너진다.
- 시험 일정은 변동되므로 "최신 정보는 공식 사이트 확인" 안내를 함께 표시.

## 8. 개발 단계 (순서 고정)
- **Phase 0 (준비)**: 폴더 구조, docker-compose로 DB, FastAPI+DB 연결 확인, React 초기화, CORS 확인, 동의어 사전 초안, 공고 샘플 수집, 자격증 데이터 초안.
- **Phase 1 (추출)**: 공고 입력 UI + LLM 추출 + 정규화 + 검수 화면.
- **Phase 2 (갭 분석)**: 역량 프로필 + 임베딩 매칭 + 갭 산출/시각화. ← 가장 무거운 핵심.
- **Phase 3 (타임라인)**: 우선순위 로직 + 자격증 일정 연결 + 액션 변환 + 타임라인 UI. ← 여기까지 MVP.
- **Phase 4 (마무리)**: 다중 공고 관리, 로그인, 온보딩, UX 마감, 베타.

## 9. 반드시 지킬 원칙
- **매 단계 끝에 실제로 동작하는 결과물이 남아야 한다.** 통짜로 만들다 붕괴 금지.
- **단정하지 말고 근거를 제시한다.** "이 기술이 부적합" ❌ → "공고가 요구한 X가 사용자 입력에 안 보임, 확인 필요" ⭕
- **인재상은 특히 단정 금지.** 성격·태도를 평가하지 않는다. "이 회사가 강조하는 가치" 안내까지만.
- **개인정보(학력·이력) 저장 최소화.** 초기엔 세션/로컬 위주, 영구저장 지양.
- **콜드스타트 완화**: 이력서 업로드 시 역량 자동 추출로 빈 화면 방지.
- **크롤링 금지.** 사용자 입력 기반만.

## 10. 코딩 규칙
- main 브랜치는 보호됨. 직접 push 금지. `feat/기능명` 브랜치 → PR → 리뷰 → 병합.
- `.env`(API 키)는 절대 커밋 금지. `.gitignore`에 `venv/`, `.env`, `__pycache__`, `node_modules/`, `dist/` 포함.
- 백엔드 패키지 설치 후 `pip freeze > requirements.txt` 갱신.
- 커밋 메시지는 `feat:`, `fix:`, `chore:`, `docs:` 접두어 사용.
- DB 스키마 변경은 Alembic 마이그레이션으로 관리.

## 11. 지금 당장 필요한 작업 (요청 예시)
"Phase 0을 세팅해줘: 위 구조대로 폴더를 만들고, docker-compose.yml(PostgreSQL+pgvector), FastAPI 기본 서버(/health 엔드포인트 + CORS 설정), Vite로 React 초기화, .gitignore, .env.example, requirements.txt까지. React에서 /health를 호출해 연결을 확인하는 최소 화면도 만들어줘. 실행 방법은 README에 정리해줘."